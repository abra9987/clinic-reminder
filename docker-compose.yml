services:
  backend:
    build: ./backend
    environment:
      - DATABASE_URL=postgresql://postgres:194db87e184041c6f072235ebf28dd12@msaxb1gvzcczmdu2aalkeugb:5432/clinic_reminder
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-clinicadmin2026}
    networks:
      - coolify
      - default
    restart: unless-stopped

  frontend:
    build: ./frontend
    depends_on:
      - backend
    restart: unless-stopped

networks:
  coolify:
    external: true
